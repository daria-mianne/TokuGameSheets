import { MenuButton } from "@components/menubar/MenuButton";

export default {
    title: 'Components/MenuBar/MenuButton',
    component: MenuButton,
    tags: ['autodocs'],
};

export const Example = {
    args: {
        name: 'A Menu Button',
        destination: '',
    }
}
